# Example environment configuration for VibeStack Security Middleware
# Copy this file to `.env` and fill in secure values for your environment.
# Never commit your real `.env` file to version control.

# Application environment: 'development', 'test', or 'production'
NODE_ENV=production

# Secret key for JWT signing (use a strong, random value in production)
JWT_SECRET={{FETCH_FROM_SECRET_MANAGER}}

# Rate limiting window in milliseconds (e.g., 60000 for 1 minute)
RATE_LIMIT_WINDOW_MS={{PROD_CONFIG_RATE_LIMIT_WINDOW_MS}}

# Maximum number of requests allowed per window per IP
RATE_LIMIT_MAX_REQUESTS={{PROD_CONFIG_RATE_LIMIT_MAX_REQUESTS}}